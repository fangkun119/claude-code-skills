## 1.1 正确的文档标题

这是文档的真实内容，应该被正确识别为标题。

```python
# 这是代码块内的注释，不应该被识别为标题
## 代码块内的井号符号
### 继续代码块内容
def test_function():
    # 更多代码注释
    ## 不应触发标题验证
    return "test"
```

## 1.2 代码块后的真实标题

这是另一个应该被正确识别的文档标题。

## 2.1 最后一个标题